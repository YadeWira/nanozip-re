# Quirks, defects and loose ends of the original `nz` 0.09 alpha

NanoZip never had a stable release: 0.09 alpha (2011) is the last build Sami Runsas
published, and it still carries the rough edges of an alpha. This page collects the ones
measured while making nanozip-re behave like the original — every item below was observed on
`nz 0.09 alpha/Linux32` (and, where noted, on the Windows build), not inferred from the
decompile. It is meant as a reference for anyone writing a compatible tool: some of these
must be *reproduced* to stay compatible, others are best left behind.

**Policy (2026-09-03):** nanozip-re reproduces the original first, defects included, so that the two
binaries can be compared on equal terms; which of these defects to keep and which to fix is a decision
for the community once the decoder is complete, and it will be taken with this list in hand. Until
then the only escape hatches are environment variables (`NZ_SAFE=1`, `NZ_STRICT_EXIT=1`), never new
switches. The verdict column says what nanozip-re does today.

| # | Area | What the original does | nanozip-re |
|---|------|------------------------|------------|
| 1 | exit status | **Always 0**, on every path: unknown command, missing archive, foreign file, corrupt data, checksum mismatch. Scripts cannot tell success from failure. | Reproduced. `NZ_SAFE=1` or `NZ_STRICT_EXIT=1` return 2 for damaged or undecodable content (`NZ_STRICT_EXIT` also gives distinct codes for usage errors). Pending the community's decision. |
| 2 | damaged archives | Writes whatever it decoded. Output is flushed per codec block (`-co`/`-cO`/`-cc`: the block is a whole type group up to the window, so a 6 MB text file yields nothing; `-cc` blocks of ~2 MB flush individually), per 1 MB stream for `-cd`/`-cD`, per member for `-cf`/`-cF`. When a block fails: the files of the blocks before it are on disk, the file the failing block starts with is **created empty** (a file it cut mid-way keeps the bytes of the earlier blocks), nothing is warned per file — only `Archive corrupted. Error decoding (code N)`. When decoding completes but a checksum fails: the wrong file is written, `Checksum mismatch [stored computed]: file` is printed and the footer follows. | Reproduced: 42 of 48 corruptions leave the same tree and 39 of 48 print the same report (see 4); the rest are decoder garbage on corrupt audio and lzpf blocks, the crashes of 27 and the store truncation of 26 |
| 3 | overwrite prompt | `Overwrite <file> (Yes/No/Always)?` reads a raw key. When stdin is not a terminal (pipe, `/dev/null`, a script) the read fails and the prompt is **re-printed forever**, at full speed — 1.7 GB of prompt text per minute in our test, until the disk fills. `-y` avoids it. | Reads a line; end of input counts as *No*. [pending] |
| 4 | error codes | `Archive corrupted. Error decoding (code N)` carries one STATUS per codec, printed **plain** when a later record made the driver notice it and as **N·256 + stream slot** when the failing record was the archive's last or the input ended (so a truncated archive says 25600, 4096, 1536, 512 …). Statuses: `-co`/`-cO`/`-cc` 100 payload check, 101 LZ output, 102 buckets, 103 inverse BWT, 104/105 param14/15, 107/108 param2/1, 109 text, 110/111 final/dece (per-stage check bytes, see 25), 2 image block, 16 audio block, 3 bucket decode, 5/7 param14/15 failing, 12 text pipeline failing, 14 output over capacity; `-cd`/`-cD` 2 CM chunk, 3 prefilter chunk, 5 token loop, 6 text pipeline, 10/11/13 output beyond total, 16 chunk header past the stream; `-cf`/`-cF` 2 header beyond input, 3 out_size over the remaining output, 4 out_size ≥ 0x8001, 5 sub-decoder, 6 block consuming more input than the stream holds, 7 output over the window; store 1. A clean-but-short `-cd` decode says `Archive corrupted. Unexpected end of file.` instead. Measured with GDB on the report method (`FUN_0804ae80`) and the codec objects' status methods (`FUN_0809c8c0`, `FUN_080aa850`, `FUN_08097e20`, `FUN_080913e0`). | Reproduced: the same channel, codes and plain/·256 rule (39 of 48 damaged cases print the identical line; the rest are the crashes of 27, the uninitialised memory of 26 and four `-cf`/`-cF` cases where the original decodes a corrupt block's garbage differently). |
| 5 | archive name | `.nz` is appended unless the name already ends in `.nz` or `.exe`, **case-sensitively**: `m.NZ` becomes `m.NZ.nz`, `m.EXE` becomes `m.EXE.nz`. `-nofilenameext` disables it. | Reproduced. |
| 6 | foreign files | A file that is not an archive is probed by reading record headers; when the first record type happens to be 14 the *next* byte is reported as a version number: `Archive file is made with incompatible version (1.12)` for a text file starting with `Nano…`, `(0.97)` for the four bytes `Nano`. Random data usually gets `File is not a NanoZip archive.`, but not always. | Reproduced (the probe is ported), since the message text is part of the interface. |
| 7 | `-` switch | Documented in the usage text as "stop scanning switches", but on `l`/`t`/`x` a lone `-` is rejected: `Unknown argument: -`. | Reproduced. |
| 8 | switch values | Values must be attached: `-o out` makes `out` the archive name (so `Archive: out.nz` / `Cannot open archive!`), `-x` alone is `Unknown argument: -x`. `-m` accepts decimals (`-m1.5g`) but `-mfoo` is rejected; `-p`, `-m`, `-o`, `-t`, `-br`, `-bw` may be empty and are then ignored. | Reproduced. |
| 9 | switches anywhere | Switches are recognised in any position after the command: `x archive -y` overwrites, `x archive -v` shows the verbose header. A file argument beginning with `-` therefore cannot be given. | Reproduced. |
| 10 | `l` ignores file arguments | `l archive some/file` lists the whole archive; the arguments are silently dropped. | Reproduced. |
| 11 | `-swapinout` / `-forceout` | Benchmark helpers with odd corner cases: with no file argument `-swapinout` makes the archive name `*` (`Archive: *.nz`), and `-forceout` writes every extracted entry to a file literally named `*`. | Reproduced. |
| 12 | `-t<n>` on decode | Caps the reported `Threads:` value; `-t0` and values above the CPU count mean "auto". It does not appear to change the decode itself. | Reproduced (and it also caps the decoder's own thread pool). |
| 13 | `Compressor #k` order | On a multi-threaded (`-pN`) archive the per-worker lines come out in **thread-scheduling order**, different from one run to the next, interleaved with the progress redraws. | Deterministic stream order (0..N-1). Only difference left on those archives. |
| 14 | container record order | The metadata records of the worker streams are likewise written in the order the threads finished, not stream order: a decoder that assumes stream 0 comes first mis-parses about one archive in twenty. | Handled (was a bug here once). |
| 15 | progress line | Redrawn from a ~0.5 s timer, only when the megabyte figure or the current file changed; the figure is *cumulative* over the whole archive, rounded (`0 MB` … `3 MB`); names over 40 columns are shown as `...` plus their last 37 characters; the name/figure/backspace choreography is fixed. The number of redraws therefore varies between two runs of the same command. | Same rules; the redraw count still differs by timing. |
| 16 | `IO-out` in the footer | `IO-in: 0.00s, 51 MB/s. IO-out: 0.00s, 738 MB/s` appears only when the write phase happened to take measurable time; on a fast machine the same archive shows it in one run and not the next. | Not reproduced (timing-dependent). |
| 17 | `[N MB]` figure | The compressor line's memory figure is the codec's own memory-usage method, which mixes window size, table sizes and fixed allocations (e.g. `nz_cm [419 MB]` at the default `-m512m`, `nz_lzpf_large [68 MB]` even for a 20-byte input); it says nothing about what the *decoder* will use. | Reproduced to the byte (transcribed formulas). |
| 18 | `-v` on decode | Adds only ` IO-buffers: 0+1 MB.` (`t`) / ` IO-buffers: 0+4 MB.` (`x`) to the compressor line and exact byte counts to `l`; nothing else is more verbose. | Reproduced. |
| 19 | listing units | The `size` column steps up to the next unit once the value exceeds *nine* of the current one (9216 B prints as bytes, 9300 as `9 KB`; exactly 9 MB prints as `9216 KB`) and rounds rather than truncates. | Reproduced. |
| 20 | self-extractors | `l`/`x`/`t` accept a `.exe` made with `w32c`: the opener parses the PE header and seeks past the image. A PE whose section table it cannot read yields `File is not a NanoZip archive.`. | Reproduced (PE offset ported). |
| 21 | checksum coverage | With `-pN` (the default above ~8 MB) a file that spans several worker streams has **no whole-file checksum** — each stream checksums only its slice, and `l` shows `n/a`. | Same; slices are verified individually. |
| 22 | `decr_param 3` | The block type the image model uses (uncompressed BMP/PGM/PPM/TGA/TIFF) is decoded by the original; the community reference decoder (`nzdec_v0`) treats it as ordinary CM and fails every such file — a documentation gap of the format, not a defect of `nz`, but the source of most third-party decoding failures. | Ported. |
| 23 | 32-bit memory | The 32-bit Linux build decodes multi-gigabyte archives fine (it streams); it prints `Out of memory!` when the *compressor's* model does not fit. | Our 32-bit builds hold the decoded stream in memory and print `Out of memory!` on archives whose content does not fit; use the 64-bit build for those. |
| 24 | `a` on the current directory | `a archive.nz` with no file arguments quietly archives whatever is in the current directory (in our test: the shell's own redirect target). | Not applicable (encode not implemented; an error is printed). |
| 25 | per-stage check bytes | Every `-co`/`-cO`/`-cc` block header carries one check byte per decoding stage (payload, entropy-decoded BWT buckets, inverse BWT or LZ/CM output, param14, param15, param2, param1, the text-transform chain): the archive's Fletcher-32 of that stage's buffer **modulo 255**, stored in stage order and consumed from the end (`FUN_080c0220` pops the last byte after each stage). A mismatch stops the block before anything is written: `code 100`, or 103 on the last stage of a BWT block. The community reference decoder skips these bytes ("staged_checksum_count"). Found with a GDB read-watchpoint on the byte. | Reproduced (one byte in 255 escapes both). |
| 26 | truncated store archive | `-cn` archive cut at 85 %: all five files are created at **full size**, the cut one and every later one filled with garbage, four `Checksum mismatch` lines (three computed as `ffffffff`), exit 0. | `Data corrupted while reading headers!`, nothing written. Not worth reproducing. |
| 27 | crashes on damaged archives | Corrupt input can kill the original: a `-cD` corruption aborts with `*** glibc detected *** free(): invalid pointer` (exit 134) after printing `Error decoding (code 6)`; another `-cD` and one `-cF` corruption **segfault** (exit 139) with no message; the outcome depends on the memory layout (under GDB with ASLR off the segfaulting `-cD` case prints `code 4096` instead). A multi-file `-cd -p4` container cut at 85 % also segfaults (exit 139), after creating some of its files. | Not reproduced: nanozip-re prints the code the original would have reached (fuzzed 1358/1358 clean). |
| 28 | archive of only empty files | `x` on an archive whose files are all zero bytes **segfaults** (exit 139) and writes nothing; an archive mixing empty and normal files extracts fine, empty files included. | Not reproduced: the empty files are created. `[pending]` |
| 29 | path traversal | An entry named `../../../x` is written **outside the extraction directory**; an absolute `/tmp/x` is written as `tmp/x` under it (leading slash dropped). No warning. | Not reproduced: the entry is skipped with `Skipping unsafe path in archive:` on stderr and counted in the footer. `[pending]` |
| 30 | directories | Every directory created on extraction has mode **0700**, whatever the umask. `a` takes file *patterns*, not directories (`a x.nz dir` is `No files found with dir`; `-r 'dir/*'` recurses), and empty directories are never stored. | Reproduced (0700). |
| 31 | timestamps | A file's mtime is stored as epoch **minus** the writer's UTC offset and read back as stored **plus** the reader's current offset, so it is only right in the writer's time zone (2·offset skew elsewhere: a 12:34 -0500 file lists as 22:34 under UTC). All of it in 32-bit `time_t`: a 2038 or 2100 mtime lists and restores as 1901 or 1963. A timestamp that comes out as exactly 0 prints as an **empty** date column and is not applied; negative ones (1969) print and apply normally. | Reproduced (skew, wrap, empty field). |
| 32 | write failures | A file that cannot be created (read-only directory, a directory in its place, `-o` naming a file or `/`) prints `Cannot write: <path>` on its own line and the run continues to the normal footer, exit 0. | Reproduced. |
| 33 | `-forcemem`, `-continue`, `-pause` | `-forcemem` is accepted with `Warning: Ignoring deprecated option -forcemem.` as the first line; `-continue` is accepted silently (undocumented); `-pause` prints `Press enter to continue...` (no newline) after the footer and reads a line. | Reproduced. |
| 34 | `Unknown command` | Two formats: `Unknown command q` for a one-character token, `Unknown command: qq` for two or more characters — and for the empty token (`nz "" archive`). | Reproduced. |
| 35 | `-fo` | With `-fo` the listing gains a `user/grp.` column (`%4u/%4u`) after the mode, only when the archive was made with `-fo` (record types 8 and 9, u16 runs); on other archives the switch changes nothing. | Reproduced (listing); ownership is not restored (needs root). |
| 36 | `l` name width | The plain listing shortens names over 40 columns to `...` plus the last 37 characters, like the progress line; `l -v` prints them whole. | Reproduced. |
| 37 | `s` and `w32c` | `s` (simulate) with a non-existent first file prints `No files found with <name>` and then simulates on the remaining arguments into `<dummy>.nz`; `w32c archive.nz` asks `Overwrite archive.nz (Yes/No/Always)?` about its own input and, with stdin not a terminal, loops forever (see 3). | Not applicable (encode / SFX creation not implemented; `w32c` prints a refusal). |
| 38 | image detectors | The image model is engaged for 24-bit BMP, 24-bit TGA, binary PGM (8- and 16-bit) and PPM; 8/16/32-bit BMP, 32-bit TGA, PBM and uncompressed TIFF are compressed as ordinary data. Its predictor always runs in mode 2 (`(left+above+1)>>1`): none of the other seven modes the decoder implements has ever been emitted (synthetic gradients and 240 real images). | Reproduced (all decode byte-exact); modes 0/1/3-7 stay ported-but-unexercised. |
| 39 | `Internal error` | Failed assertions inside the engines print `Internal error: <id>! Please report this to sami (at) nanozip.net.` (after a newline, the status line is not cleared) and exit **255**, the one status not laundered to 0 (see 1). The ids are constants of the code: 21231532 is nz_lzhds' reconstruct finding a match longer than the chunk (a corrupt or truncated `-cD` stream), 9256352 an unknown codec byte, 100 is `Out of memory!`; 22563525 appears from a thread-teardown race on some truncated `-cd` archives (one run in three). | Reproduced for 21231532 (same line, exit 255); the race is not. |
| 40 | `-cO` LZP table wrap | When the ring scrolls, the LZP hash table is cleared except its **last 16 entries** (hashes 0xfff0–0xffff), which keep stale positions; a later hit on them resolves against the new ring contents. | Reproduced (`nz_optimum2_lz.cpp`, the wrap sweep). |
| 41 | parallel containers on damage | A `-pN` container is written by its **workers**, each straight into the output files at absolute offsets (one `open` per worker on the same file). A worker whose stream fails leaves a **hole of zeros** the size of its slice (the file keeps its full length when a later slot wrote past it; it ends short when the failing slot was the last). The CM family flushes per codec block (a failing slot keeps its completed blocks), lzpf per member segment and `-cd`/`-cD` per file group: nothing of the unit in flight survives a status error, but a stream that merely ends short flushes what it produced. A group's first file is created, empty, when the worker starts the group; later files when bytes reach them; groups after a failure never appear. The other workers **carry on**, so a damaged 16-stream archive still writes 15 good slices. Slice checksums are verified as each slice completes and a mismatch prints `Checksum mismatch [stored computed]: <file>` (the slice's values under the file's name), then the normal footer, exit 0; a status error prints `Archive corrupted. Error decoding (code status·256 + slot)` and no footer. A data record cut by the end of the archive is dropped (the compressed workers never see it; a `-cn` worker copies its bytes and zero-pads the slice), and the cut worker reports a codec-specific short-end status: `-cc`/`-co` `Unexpected end of file`, `-cO` 100, lzpf 2 (4 when a whole group had no data left), `-cd`/`-cD` 4 (1). | Reproduced (the sink in `sfx_archive.cpp`, `namespace psink`; measured on 8 codecs × 7 corruptions × single- and multi-file: files identical in 100+/112, see README). |
| 42 | lzpf worker overruns | Inside a parallel stream the lzpf worker does not stop when a block's side data runs past its record or its output would overshoot the slice: it clamps, produces garbage and reports at the **next** block header (`code 4` or `2`), where the single-stream decoder reports the overrun itself (`6`, `3`). | Reproduced (`DecodeLzpfMember` lenient mode for parallel streams). |
| 43 | timestamps past 32 765 files | A file's mtime is stored as one absolute u32 followed by a zigzag delta per file, and a run longer than a record's payload (~32 KB) is split across several type-2 records. The original reads each record as a fresh run, so from the first continuation record on it takes four delta bytes as an absolute value near zero: on a 70 000-file archive the entries from 32 765 on are listed (and restored) as `1969-Dec-31`, climbing a second wherever a delta says so, and the **last six** entries get no date column at all because its value run ended six short. | Reproduced, listing and restored timestamps included (70 000/70 000 identical). |
| 44 | the same file twice | `nz a x.nz f f` writes ONE entry whose size covers both copies, and in a `-pN` container both copies get the SAME slice offsets, so the workers overwrite each other: the extracted file is one copy long while the footer counts every decoded byte. | Reproduced (the sink writes each slice where its stream says; the eight codecs all match). |
| 45 | symlinks and unreadable files | `a -r` over a tree **silently skips every symbolic link** (to a file, to a directory, absolute -- neither the link nor a copy of its target reaches the archive) and every file it cannot open, so a mode-0000 file is simply absent with no message and no non-zero exit. Setuid/sticky bits on the files it does take are kept (a 04755 file lists as `4755`). | Not applicable to the decoder: nothing to reproduce, since such entries never exist in an archive. Our extraction of trees carrying them is identical to the original's (`tests/sweep_dirs.sh`, 16/16 archives). |
| 46 | the `[N MB]` figure follows the block, not the window | The memory figure on the `Compressor #k` line is computed from the BLOCK size the compressor settled on -- the codec record's third byte -- and not from the LZ window in its second. Two `-co` archives of the same input with the same window byte print 13 MB and 16 MB when they were made with `-m8m` and `-m24m`, because their blocks are 1 MB and 3 MB. | Reproduced: `-co`, `-cO` and `-cc` archives of one input at `-m8m`, `-m24m`, `-m32m`, `-m48m`, `-m64m` and `-m128m` all print the original's figure. |
| 47 | a filename table cut mid-record | An archive truncated inside its filename table still lists: the original reads the names that fit **and keeps going past the end of the file**, so a 64-byte header lists about thirty entries, most with empty names and sizes that come from whatever follows the buffer, before `t` reports `Archive corrupted. Unexpected end of file.` | Partly reproduced: we keep the entries that are complete (which the previous version rejected outright with `Data corrupted while reading headers!`) and then report the truncation, but not the entries the original builds out of memory past the end -- the same line as quirks 26 and 28. |
| 48 | Windows archives store attributes, not modes | An archive made by the **Windows** original carries file attributes instead of POSIX permissions: record type 3, one nibble per entry, `8 \| READONLY \| HIDDEN<<1 \| SYSTEM<<2`, high nibble first and zero-padded, and the record is **omitted for a block whose files are all plain** -- so a multi-block archive has it for some blocks only, and the entries it does not cover list as plain. The Linux original maps the attributes to a mode when it lists or extracts such an archive -- **0400** for read-only, **0600** for everything else -- while a Windows build names the column `attr.` (a header word one character wider than the values under it), prints `R`, `H`, `S`, `A` in fixed positions, and on extraction **restores those attributes**, so a read-only, hidden or system file comes back that way. An archive carrying POSIX modes instead leaves the field blank. | Reproduced on both platforms -- listings, restored modes and, on Windows, the restored attributes (`tests/windows_original.sh` over wine, and the same comparison run on a real Windows 10 machine). |
| 49 | the thread count comes from the CPU, not the OS | `Threads:` reports the CPU's physical cores read from the CPU itself: on a 16-core/32-thread host the original says 16 on Linux **and** under wine, where the Windows API reports 32 single-thread cores. | Reproduced: the Windows build derives the hyper-threading ratio from CPUID (logical per package / cores per package) instead of trusting `GetLogicalProcessorInformation`. |
| 50 | a truncation the decoder can absorb | An archive cut so that its first data record still ends at a **whole codec unit** decodes that much and reports **success**: `-cf` cut at 40 % writes the first entry complete (120 000 bytes), prints `Decompressed 120 000 bytes` and the footer, exits 0, and the other four entries are simply absent -- no `Archive corrupted` line, no warning, no non-zero status. Cut a few percent earlier, inside a block, the same archive says `Error decoding (code 512)`. Silent data loss on a damaged archive. | Not reproduced: we report `code 512` there. Our decoder treats a member that ends before its declared output as a failure; the original stops cleanly and calls the short output the whole archive. |
| 51 | before the decoder exists, it is always EOF | A cut that lands before the first data record's **header** is complete -- in the file's metadata, in the filename table, or in the record framing itself -- is reported as `Archive corrupted. Unexpected end of file.` and no `Compressor #k` line is printed at all, because the decompressor object is created only when a data record is dispatched. Once that header is whole, and for any cut inside the FIRST data record, the report is the family's short-end status: `-cd`/`-cD` 1024, `-cf`/`-cF` 512, the CM family 25600 (`-cc`/`-co`/`-cO` still say `Unexpected end of file` until 5 payload bytes exist, the size of their block header); `-cn` prints nothing. A cut in a later record reports whatever the decoder makes of the cut block. Measured byte by byte around the first data record of `m_<codec>.nz`, all eight codecs. | Reproduced: the EOF rule, the suppressed `Compressor` line, and the first-record statuses (`tests/parity/truncation_sweep.sh`: 105 of 152 cuts across the eight codecs print the identical line; the 28 that differ are all cuts beyond the first data record of `-cd`/`-cD`/`-cf`/`-cF`, where the original decodes the cut block's garbage and reports whichever check trips first -- 4 where we see 6 for lzpf, 1536/3840/256 or the lzhds assertion for `-cD` -- and the 19 of `-cn`, quirk 26). |
| 52 | the order a parallel archive lists its files | A `-pN` container lists its files in neither the order they were given to `a` nor the order its records name them. Each worker stream keeps its own entry list in table order, and the streams are **merged into the global list one at a time, in stream-id order**: the merge walks a stream's list collecting a run; a slice of a file split across streams (its table is followed by a slice-offset record) ends the run, which is spliced at the **head** of the global list keeping its internal order, and the slice is either added to the entry of the same name already listed (size summed, checksum zeroed -- the `n/a`) or starts the next run. So `-p2`, `-p3` and `-p4` of the same five files list in three different orders, deterministically. | Reproduced (the merge of `FUN_080922a0`, read with GDB: a watchpoint on the list head and a dump of both lists at every call; recipe in the wiki). `-p2/-p3/-p4` of one set, the seven-file fixtures and every codec x checksum mode (240/240) list in the original's order. |
| 53 | `-x` and unreadable files are "opened" anyway | A file that matches an `-x` pattern -- or that simply cannot be read (mode 000) -- is not removed by the scan: it stays in the list, its size counts when `-pN` splits the work and when the window is chosen (`a -cn` of a 60 000-byte file next to an unreadable 40 000-byte one stores one block with the 131072 window, not 65536), and when the reader reaches it the console says `Cannot open: <path>` -- for the `-x` match too, though nothing was wrong with it -- and the file is left out of the archive. `-x` is matched against the path as scanned, so `-xb.bin` excludes an explicit `b.bin` but not `./b.bin` under `-r .`, and `-x*.dat` excludes `sub/c.dat` while `-xc.dat` does not. | Reproduced (`tests/encode/oracle.sh` cn_x*, cn_unread*: archives and consoles identical). |
| 54 | a WAV's header comes back as the prefix of every audio block | With `-cf`/`-cF` on a WAV or AIFF the first block copies the header bytes verbatim (the prefilter "prefix") -- and so does every later block of the chunk, copying that many bytes of AUDIO verbatim instead: the compressor keeps two copies of its format probe, the analysis job's and the codec's, and the codec's (where the block encoder records the alignment carry that should follow) is overwritten by the job's before each block. Harmless to decode, wasteful to store. | Reproduced (`tests/encode/oracle.sh` cf_wav, cf_media; 54 corpus audio files identical). |
| 55 | the image compressor's gate reads uninitialised stack | `-cf`/`-cF` route a block to the image model when a BMP/PGM/PPM/TGA/TIFF header sits at its start (or the previous image has rows left); the block encoder (FUN_08089a80) first runs a gate over the first 1016 bytes -- a vertical-delta sum, an LZ match count through a 65536-entry hash table, then Huffman-cost tests -- and that table is a 256 KB stack local that is never initialised. The same frame reuses it as the residual planes, so the match count depends on the previous image block's residuals (zero on the thread's first block); a deeper frame in between (the audio encoder's 363 KB) leaves its own garbage there. The decision (image block vs literal) can flip on it. | Reproduced for image-after-image and first-block cases (the port keeps the table per stream and fills it as the original does: 24 corpus images x 2 variants and the oracle's image cases byte-identical); the audio-frame garbage is not reproduced (no corpus case reaches it). |
| 56 | `-cd` text detector's empty table | The `-cd` text pipeline decides whether the word dictionary pays with a detector that scores candidate words against a word-continuation table -- and that table is filled by the dictionary TRANSFORM itself, the first time it runs in the process. So the first chunk a process examines is scored against an all-zero table (every continuation "unknown") and later chunks against the filled one: the same 32 KB of text can be transformed or left alone depending on whether the process has already transformed some text before. | Reproduced (the port fills the table at the same moment: oracle cd_text, cd_crlf, cd_pgn and 64 mixed corpus files identical). |
| 57 | unreadable files under the compressing codecs | With `-cf`/`-cF`/`-cd`/`-cD` the reader keeps an unreadable file's SIZE as the current entry's byte budget after the open fails, and fills it from the files that follow: `unread.bin` (5000 bytes, mode 000) before `b.bin` (30 000) stores b.bin in two DATA records of 5000 and 25 000 bytes, its table entry announced with the first; a second ghost met while filling adds nothing; when the budget is spent the piece-boundary rule runs with the file then open as "the file just read". The progress figure drops the ghost from its total at the moment the open fails (17 % = 5000 of 30 000 after that first piece; 82 % = 30 000 of 35 100 when the ghost has not been met yet). The store reader has no such budget (quirk 53). | Reproduced (29 shapes under `-cd`, 4 under `-cf`, `-r .` over the fixture tree with an unreadable file: archives and consoles identical). |
| 58 | archives are not reproducible above one thread | With the default thread count the reader and the compressor run concurrently, and how many small files the reader has in hand when the compressor takes a piece depends on timing: `a -cd a.txt b.bin` (30 KB each) gave a one-piece archive in three runs of eight and a two-piece one in the other five -- the compressed bytes identical, the file table laid out differently. And a file's checksum record is written when the READER thread finishes the file, so on a two-piece 1.2 MB file it precedes both DATA records under the default threads (the reader is 1 MB ahead) and follows them under `-t1`. Under `-t1` everything is deterministic. | Not reproducible by definition; the port groups the `-t1` way at every thread count, and the encode oracle runs the original under `-t1`. |
| 59 | a long match prices as free | The `-co` optimal parser prices a match length through a cache indexed by `len - 2`, and the whole pricing block sits under `if (len - 2 < 0xff)`. A length of 0x101 or more therefore falls through with the accumulator still at zero, so the parser believes the longest matches cost nothing at all and takes them over anything shorter regardless of the distance they need. Harmless to decode -- the coder still writes the real bits -- but it is the cost model lying to the search. | Reproduced (`LenCost` in `src/nz_optimum_lz_parser.cpp` returns 0 on the same condition). |
| 60 | the price caches collide | The same parser keeps four lazily filled price caches inside its model, and two of them are indexed more coarsely than the price they hold. The length cache's key is `(len - 2) * 2 + (slot is rep0)`, so a rep slot 1-3 and a brand-new distance share a row although their prices start from different tree seeds; the distance-class cache's key is the distance's bit length plus the two bits under it, while the price it stores was computed for one particular **length** bucket, so whichever length asked first fixes the price every later length reads; and the rep-select cache is keyed on three bits of the history byte while the model cell it prices is chosen by four, so two different histories share one stored price. Both make the search compare numbers that were never about the case at hand. | Reproduced (same keys, same fill-once rule; the caches match the original's byte for byte on the fixtures that agree). |
| 61 | param1's cost estimator counts a match as the byte 0x80 | Before accepting a delta region the `-co` compressor prices it twice with an order-2 LZP over a Huffman histogram, and the counter it bumps on a correct prediction is **bin 128 of that same 256-bin literal histogram** (in the original a separate stack local that the frame lays exactly on `freq[128]`, and the clearing loop covers it). So every LZP hit and every literal `0x80` share one frequency, one code length and one price. Both passes (raw and delta) are scored the same way, so the comparison is consistent -- but the number it compares is not the cost of either encoding. | Reproduced (`freq[128] += 1` on a hit in `src/nz_optimum_param1.cpp`), which is what makes the two open KML files pick the original's regions. |
| 62 | param1 measures outside the block | Three of param1's measurements read outside the data they are given. The profitability scorer's opening probe always scans **512 bytes forward from the candidate position** even when it was called to extend *backwards*, so a candidate near the end of the block reads into whatever follows it in the codec's buffer; the positional sort keys on the 32-bit word at `data + index` and reads out `data[index + 5]`, i.e. up to five bytes past the window it was handed; and the start of a block is delta-coded against `data[-offsetA]` / `data[-offsetB]`, up to 255 bytes **before** the block (the two offsets are 1 and 2 only until the first re-evaluation, ~100 qualifying bytes in). In the original those bytes are the rest of its own block allocation, zero on a fresh buffer, so the decision is stable in practice and undefined in principle. | Reproduced by running the pass over a zero-padded copy of the block, which is what a fresh allocation gives the original; no memory outside our own buffer is touched. |
| 63 | param1's region cache is half uninitialised | The driver caches, per (offset, alphabet-size) pair, the region it already measured so a later decision at the same place is skipped. Only the pair's **end** field is cleared at start-up; the **start** field is stack garbage. The test is `end <= pos || pos <= start || start < lastEnd`, and with `end` zero the first term is always true, so the garbage is never actually read -- the code is correct by short-circuit, not by construction. | Reproduced (both fields cleared; the short-circuit makes it equivalent). |
| 64 | the exe filter can make a block bigger, and the driver takes it anyway | dece drops each transformed displacement (-4 payload bytes) but adds one add-esp byte and, for a target it has not seen recently, four raw target bytes: a new-target CALL is a net **+1 byte**. A block whose calls are nearly all first-sightings therefore comes out longer than it went in, and the analysis only tests the return value for zero -- it never compares it against the input size -- so the longer form is what gets compressed and stored. | Reproduced (the writer takes any non-zero length, and sizes its output buffer for the growth instead of trusting it not to happen). |
| 65 | the exe filter's address base wraps at 23 bits | The compressor accumulates the run's output position with `base = (base + n) & 0x7fffff` (FUN_080b98e0), so a run of consecutive dece blocks whose output passes 8 MB silently restarts its reference frame -- and every target already in the recent-target cache is then 8 MB out of step with the ones computed after the wrap, changing which cache hits the coder finds. | Reproduced on both sides. The decoder had no mask (it just added), which would have disagreed with the compressor from the wrap onwards; the corpus never reaches an 8 MB dece run, so this was found by reading the encoder, not by a failing file. |
| 66 | the exe filter reads ahead, then checks | At each 0xe8 / 0xe9 / 0x0f 0x8x the compressor loads the 32-bit displacement **before** testing that the instruction fits in the block, so it reads up to 4 bytes past the end (and one past, for the byte after a trailing 0x0f). Its add-esp side area is sized `n/8`; overflowing it writes over the call-target area that follows -- and the filter then declines anyway, so the corruption is invisible. And if the arithmetic stream ever fills half of its 0x108001-byte budget the compressor calls its internal-error abort rather than declining. | Reproduced except the abort: we run over a copy padded on both sides, take the overflow decision without the corrupting writes, and decline where the reference would abort (unreachable -- half of 0x108001 bytes is far more arithmetic stream than a block can produce). |
| 67 | param14's match finder starts from a hash table nobody cleared | The BWT-only LZ77 pass sizes its hash and chain tables into the block buffer past the 2n mark and never initialises them, so the first call of a run arrives with whatever that memory last held -- 22 211 live entries, measured. It is harmless rather than lucky: a stale entry still has to name a position before the current one, carry the right high-bit tag, and then survive a four-byte comparison, so it can only ever produce a match that is really there. Seeding our own table from a dump of the reference's changes nothing at all (measured on the same block, byte for byte). | Reproduced with a zeroed table, which is what the invariant above says it may be. |
| 68 | one threshold table, read at two offsets | param14 vetoes a match whose bytes are too common: it sums a per-context rarity count across the candidate and compares against a length-indexed threshold. There are two thresholds, and the second is not a second table -- it is the same one read six entries further along (`DAT_081b3280` and `DAT_081b3298` are 24 bytes apart), so the "is this match worth a tag when the offset is a repeat" test is literally "would a match six bytes longer have passed the strict test". | Reproduced. Both tables are generated, not embedded: `(n-3)^2 * ((n-5)/3)` and `max(1, (n-4)/2)`, verified 249/249 and 250/250 against a dump from a live process. |
| 69 | a stored block drops two header fields, one of them silently | `param6 == 0` means the block has no compressed layer, and then **size18 is not written at all** -- the block expands to exactly its payload size. `param7` disappears with it (it is read only under `if (param6)`), so a stored block runs staged bytes -> `bwt_start_pos` with nothing in between. The reference's own decoder still reads `size = header.size18` unconditionally on the LZ branch, where size18 was never assigned and `Header` has no constructor: with `param6 == 0` it is reading an uninitialised field. What the real binary does is take the payload size, and that is what makes the sizes add up. | Reproduced on the writing side (the fields are omitted) and on the reading side (payload size, not the uninitialised field). Writing the param7 byte anyway shifted every later field: five stored blocks made the archive exactly five bytes too long and `bwt_start_pos` came out as 4 166 912. |
| 70 | the bucket coder's encoder and decoder walk to different ranks | After each symbol the BWT bucket coder moves it to a new rank and codes the gap. The ENCODER finds that rank by walking the rank list against a FIXED threshold (the symbol's next position); the DECODER walks the same list against a threshold that grows by one for every entry it passes (`new_c += 1`). The two agree exactly while the list's positions are strictly increasing, which is the ordinary case -- and where they do not, the encoder walks too far and the gap it codes goes NEGATIVE. Measured on a real 18 685-byte bucket: a rank of 11 with only 2 positions of room, i.e. a coded gap of -9, which the decoder then reads as a rank delta of 4 294 967 287. | Not reproduced, deliberately. Our encoder solves for the rank the DECODER will pick (the fixed point of `k = DecoderRank(next - k)`), which is identical on every stream the reference itself produces and is the only form that round-trips otherwise. Reproducing the fixed threshold made every BWT block over roughly 18 KB of rank data unreadable. |
| 71 | the text transform's entropy estimate sorts backwards and measures four bytes late | The trial that decides the `-co` number transform prices a buffer by sorting its positions by an order-4 context and move-to-front coding the byte that follows each. Two things in it are wrong and both are load-bearing. The comb sort's compare is `key(out[i]) < key(out[i+gap])` -- it swaps when the earlier entry is the SMALLER one, so every bucket comes out in DESCENDING context order. And the bytes it measures start four past the pointer the caller hands it: the caller passes `buf + half` with length `len`, and `FUN_080522c0` histograms `buf[half+4 .. half+4+len)`, sorts on `buf[p..p+3]` and codes `buf[p+5]`. So the estimate is of a window shifted four bytes forward from the one the caller means. | Reproduced. Sorting ascending, or measuring the window the caller means, moves the estimate by well under a percent -- and that is enough: on `011_COMBO_LOCK_A.BAK_` the trial is `(3984+901)*32 < 4741*33`, true by 133 parts in 156 000. Getting it right turns that into `(4013+901)*32 < 4759*33`, false, which is what the original decides. All eight of that file's estimates now match the binary exactly. |
| 72 | the optimal parser's match-finder tree is scratch another coder writes over | The `-co` parser's binary tree lives in a 3 MB buffer the codec object also lends out. Between two blocks -- with no call to the finder in between -- roughly 4 000 of its words change under it: a hash-chain match finder at `0x080b9bc3`, running for the text transform's trial compression, uses the same memory as its own chain array (`head[(w>>19)^w & mask] = pos|tag` then `chain[pos & mask] = old head`). The parser then starts the next block on a tree seeded with another coder's links, and its walk confirms or abandons different entries because of it. Watched with a GDB write watchpoint on one word of the tree, which is how the writer was found. | Not reproduced. Our tree is only ever written by the finder, so it is *cleaner* than the original's and the walk occasionally reaches one entry further -- it answers with a match one position earlier than the original does. Costs one file in 289 (`150_menu.tbk_`), where a length-78 match at distance 36 451 is found at output position 193 of block 3 instead of length 77 at 194. |
| 73 | the elapsed time is built by hand, from milliseconds | There is no printf format for a duration anywhere in the binary: `FUN_0804bf50` assembles the string a character at a time from a count of MILLISECONDS. Hours and minutes appear only when non-zero and are never zero-padded, the seconds field is one digit under ten, and the fraction is two digits TRUNCATED from the millisecond remainder -- `1.05s`, `45.94s`, `37m 45.94s`, `2h 3m 4.56s`. The quirk is that the fields are independent: two hours and four seconds prints `2h 4.00s`, the minute field skipped outright rather than shown as zero. | Reproduced (`FormatElapsedMs`). This port printed `%.2fs` of the whole elapsed time in all three footers, which agrees only below one minute and even there rounds where the original truncates. Nothing in the corpus ran for a minute, so the branch was never taken until a 4.6 GB archive took 38 of them. |
| 74 | the 32-bit Windows banner saturates at 4 GB | The banner's `<available>/<total> MB` comes from `GlobalMemoryStatus`, whose DWORD fields report 0xFFFFFFFF on a machine with more than 4 GB, so the 32-bit Windows build prints `4096/4096 MB` on a 64 GB desktop (a user's paste, i9-11900K, Windows 11). The Linux build reads `sysinfo` and prints the real figures (`35002/64170 MB` on the same class of machine), and the 64-bit Windows build does too. | Reproduced in 32-bit builds only: this port's x86 `.exe` printed the real `6476/8172 MB` on an 8 GB machine; it now saturates both fields to 4096 when they exceed 4 GB. Its `MHz` field is not reproduced (the original printed `60214 MHz` for a 5.3 GHz i9 -- an overflowed reading this port measures correctly). |
| 75 | the passes read past the block, because the buffer is the codec's | `FUN_080bb3a0` (param14) loads a 32-bit word per position and compares candidates at `cur - 3`, so it reads up to three bytes past the last byte it counts; `FUN_08083570` (param15) reads 256 bytes past the block to prime its rolling hash; `FUN_0806e7a0` (param1) probes `data[-1]` and `data[-2]` at the very start. None of that is a bug in the reference: every pass runs over the codec's own block buffer, so the reads land on its own memory -- whatever the previous operation left there, which for param1's front probes is measurably the previous block's bytes, dword-reversed by the BWT's in-place pass. | Reproduced by giving each pass a padded copy at the call site rather than by changing the passes: param15 gets 512 bytes of the following input (zeros at the segment's end), param14 a copy with 16 bytes of slack, param1 the `kFrontPad` copy padded on both sides. The front padding is ZEROS where the reference sees stale bytes -- which is why `081_AWSOFTWA.PLA_` still differs. ASan found the param14 over-read on 5 of the 289 corpus inputs the day param15 started handing it an exactly-sized vector. |

## Things that are *not* defects but surprise people

**Format corners no encoder emits** (checked 2026-09-04, so a decoder that declines them is not
incomplete): the text-transform dispatcher understands one bit more than the seven the CM family
uses — 0x80, which means "apply param14" — but the CM block header has its own param14 flag and
across 9 255 CM blocks of the 3037-file sweep bit 0x80 never appeared while that flag was set 1 272
times. In `-cd`/`-cD` the same dispatcher can in principle also run the tt16-number (0x10) and
insert-linefeed (0x02) transforms, but both read a dedicated arithmetic side stream off the codec
object — the original itself returns "decline" for 0x10 when that side object is null — and a `-cd`
chunk header has no field to carry one; over the whole sweep the `-cd` text parameter only ever took
values built from 0x80, 0x40, 0x20, 0x08 and 0x01, and a file that makes `-cc`/`-co` select tt16 +
html makes `-cd` apply no text transform at all. The BWT's rank-delta prefix saturates at 31 bits,
which would need a single bucket above a gigabyte: on a 221 MB text input at `-m256m` and at `-m2g`
(the largest memory the original accepts) the biggest bucket is 440 054 bytes and the biggest prefix
18 bits, and the bucket size does not grow with the memory budget. The run-length base table the
prefix indexes is not a mystery either: it sits in `.bss` and the loop at 0x080c0320 fills 33 words
with 0, 2^1 … 2^31, 0.

- **The progress percentage is computed in kilobytes OR'd with 1**: `((done>>10)|1)*100 / ((total>>10)|1)`,
  so a two-block 75 501-byte store reports `89%` after its first 65 536 bytes (65*100/73) where 86 % is
  the truth, and the figure is redrawn at most once per whole second of `time()` -- a run under a second
  shows exactly one figure, the state after its first block; `-pN` under `-t1` shows an empty field
  because the first slot to be displayed is worker 0's and worker N-1 runs first.
- **Compression is not reproducible byte-for-byte across thread counts**: the same input
  compressed with `-p1` and with the default gives different archives (different stream
  splits). Decoding either is fine.
- **The window size is a byte, not a number**: `bytefloat(p1 + 1)` in 64 KB units (a 4-bit
  mantissa and exponent) is what every codec derives its window/dictionary/ring from, so
  the actual window is rarely exactly what `-m` asked for.
- **`-m` buys less window than it says**: the window is the byte-float rounding of the smaller of
  the input size and the budget reduced per codec -- the budget itself for the store and `-cf`,
  11/16 of it for `-cd`, 17/32 for `-cD`, and for `-cF` the budget minus 64 MB, so every `-cF`
  run under `-m64m` gets a 64 KB window (and `-m70m` a 6 MB one). Rounding is to the nearest
  representable value, so `-cf -m300k` uses a 320 KB window.
- **`-m` is divided by the worker count before anything else looks at it**: `FUN_0804cec0` is
  called once per worker with the budget AND the input already divided, so `-cf -t2 -m8m` gets a
  4 MB window, not 8 MB. The divisor is the number of WORKERS, not the `-t` value: a single `.wav`
  at `-t4` is media-heavy, so one worker takes the whole budget, while the same bytes under a
  non-media name get a quarter of it.
- **`-co`'s 64 KB window floor is applied after the block size has already been computed**: the
  block rule consumes the UNFLOORED window, which is zero for any budget at or under 16 MB. That
  zero makes `window - 1` underflow to `0xffffffff`, whose `bsr` is 31, which reserves a full
  1 GB; the reserve then drives the available space negative, it is masked to zero, the quotient
  is zero, and the 1 MB floor plus one byte-float step is what finally lands -- 17 units of 64 KB.
  So the familiar "small budgets always give a 1 114 112-byte block" is not a special case for
  small budgets at all: it is one uniform formula reading a window that has not been floored yet.
- **The `-co` block size is stored one byte-float step BELOW what the codec uses**: the archive
  holds `bf_enc(block) - 1` and every reader decodes `bf_dec(byte + 1)`, and the writer itself
  rounds to the NEAREST representable size (ties up) before taking that step. Rounding down
  instead -- "the next representable value above" -- gives the right answer only when the block
  happens to be representable, and lands one step low otherwise.
- **A `-co` block at or above 16 MB is never stepped up**: the step is applied only to a block
  that is both below the input and below 16 MB, so the two regimes meet with a discontinuity.
- **Filenames are bytes in the machine's ACTIVE CODE PAGE, not UTF-8**: the Windows original calls
  the ANSI Win32 API, so an archive made on a Hungarian Windows stores CP1250 bytes and one made on
  a Russian machine CP1251 -- and `l` prints those bytes raw, so the listing only reads correctly on
  a machine with the same code page. The same archive on a Linux original goes through unchanged,
  which is why the two agree byte for byte. A reimplementation that treats the bytes as UTF-8 cannot
  even form the path: reported by xman on Win7 x64, where the published v0.14.0-pre crashed with
  `filesystem_error: Cannot convert character sequence` on his accented .mpg, and refused to FIND a
  file with an accented name when compressing.
- **No platform or OS is recorded anywhere, but the metadata record TYPE gives it away**: there is no
  "created on" field of any kind -- no OS name, no architecture, no distro, and nothing from NTFS
  beyond what is listed here. What does differ is WHICH metadata record the archive carries: a
  Windows-made archive stores file ATTRIBUTES (type 3, the readonly/hidden/system/archive nibbles)
  and a POSIX one stores PERMISSION bits (type 4). So `l` prints a `0664`-style column for an archive
  made on Linux and none for one made on Windows, and that is the only platform signal in the format.
- **The original cannot store a filename its machine's code page cannot express**: it calls the ANSI
  Win32 API for the directory scan too, so on a Western (CP1252) Windows a Cyrillic, Chinese,
  Japanese, Arabic or Hindi name comes back from the scan as `???` and the original then reports
  `Cannot open: ??? ????.txt` and leaves the file OUT of the archive. German and Nordic names work
  there because CP1252 covers them; on a Hungarian (CP1250) machine the coverage is different again.
  This is not a limitation a reimplementation can quietly improve on: measured on nine languages,
  this port's scan uses the wide API, so it OPENS all nine and stores six of them under the same
  mangled `???` names -- two different files (Russian and Hindi) collapsing onto one stored name.
  The original's refusal loses nothing; ours loses the distinction. [pending: match the refusal]
- **The version string is a record**: an archive begins with a type-14 record holding
  `NanoZip 0.09 alpha` and a type-30 record holding the byte 9; the "incompatible version"
  message reports that byte divided by 100.

## How these were measured

Every item above comes from driving the original and nanozip-re through identical invocations and
comparing stdout, stderr, exit status and the tree of written files (contents, mode, timestamp, size,
path). The harnesses now live in the repository, under `tests/`, so a clean clone reproduces the
numbers; `tests/parity/make_fixtures.sh` builds their archives with the original rather than shipping
them.

| harness | what it drives |
|---|---|
| `tests/parity/matrix.sh`, `matrix2.sh`, `matrix3.sh` | 182 command, switch and shape cases: hand-built trees, empty files, links, odd names, modes, extreme timestamps, hostile paths, `-o`/`-x`/`-sp` corners, time zones and locales. `compare.sh` scores them, separating the status-line writes from real differences |
| `tests/parity/pty_prompt.py` | the overwrite prompt through a real pty, key by key |
| `tests/parity/corrupt_compare_all.sh` | one flipped byte at five offsets and a truncation, on a multi-file archive of every codec: 48 cases, `x`, `l` and `t`, trees and report lines |
| `tests/parity/corrupt_compare_parallel.sh` | the same for parallel containers, single- and multi-file: 112 cases |
| `tests/sweep_dirs.sh` | directory trees with symlinks, unreadable files and setuid/sticky modes, every codec plus `-fo` |
| `tests/sfx_exe.sh` | self-extracting `.exe` archives of every codec |
| `tests/windows_original.sh`, `tests/parity/windows_vm_check.ps1` | archives made by the **Windows** original, through wine and on a real Windows machine |
| `tests/env_switches.sh` | the environment switches this port adds |

The reverse-engineering side (GDB scripts, Ghidra passes, the per-stage dumps) is described in the
wiki's Reverse-Engineering-Notes.
